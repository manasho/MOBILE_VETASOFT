name: flutter_vetasoft
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  flutter_secure_storage: ^9.0.0  # ← CORREGIDO: misma indentación que las demás
  
  cupertino_icons: ^1.0.8
  
  http: ^1.1.0
  connectivity_plus: ^5.0.2
  
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  
  provider: ^6.1.0
  
  cached_network_image: ^3.3.1
  dio: ^5.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true