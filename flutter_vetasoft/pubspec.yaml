name: flutter_vetasoft
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  flutter_secure_storage: ^9.0.0
  cupertino_icons: ^1.0.8
  
  # API e Infraestructura (Union de ambas ramas)
  dio: ^5.9.2
  google_fonts: ^8.0.2
  intl: ^0.20.2
  jwt_decoder: ^2.0.1
  http: ^1.6.0
  dotted_border: ^3.1.0
  image_picker: ^1.1.2
  connectivity_plus: ^5.0.2
  shared_preferences: ^2.2.2
  provider: ^6.1.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/